name: shared-metrics
description: Included workflow — tokenizes a CSV list and counts a keyword.

steps:
  - transform: {id: split_items, action: split, input_key: raw, sep: ",",
                output_key: items}
  - transform: {id: flag_keyword, action: contains, input_key: raw,
                value: urgent, output_key: has_urgent}
